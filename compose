version: "3"
services:
  netflowips:
    network_mode: host
    dns:
      - 8.8.8.8
      - 8.8.4.4
    container_name: netflowips
    restart: "unless-stopped"
    image: netflowips:v0.0.1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:

